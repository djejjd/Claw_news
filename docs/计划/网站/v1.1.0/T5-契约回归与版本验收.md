# T5 契约回归与版本验收

---
状态: review_pending
最后更新: 2026-08-16
关联:
  - ../v1.1.0-公共内容API.md
  - ../../../架构/公共内容API设计.md
  - T1-公共读取仓储与契约.md
  - T2-日报API.md
  - T3-新闻流分页与过期清理.md
  - T4-来源API.md
---

## 任务元信息

| 项目 | 内容 |
|---|---|
| 任务编号 | `v1.1.0-T5` |
| 依赖任务 | `v1.1.0-T1` 至 `v1.1.0-T4` 均已完成并通过主审核 |
| 允许并行 | 无；本任务统一验证已定型的版本契约 |
| 允许修改路径 | `tests/test_public_api.py`, `tests/test_publication_store.py`, `tests/test_app_api.py`, `tests/test_ingest_job.py`, `docs/README.md`, `docs/计划/待办.md`, `docs/计划/网站开发版本计划.md`, `docs/计划/网站/v1.1.0-公共内容API.md`, `docs/计划/网站/v1.1.0/T5-契约回归与版本验收.md` |
| 禁止修改路径 | `app/**`, `alembic/**`, `collectors/**`, `aggregator/**`, `pusher/**`, `infra/**`, `frontend/**`, `pyproject.toml`, `CHANGELOG.md`, `.github/**`, `Dockerfile`, `docker-compose.yml` |
| 启动审查结论 | `approved`（2026-08-16：独立启动审查复核已批准公共内容 API 设计基线、T1-T4 completed/approved 依赖、精确允许/禁止路径、三端点及两轮清理回归矩阵、PostgreSQL 未验证待办与隔离分支；当前无工作区或并行冲突，准许启用 T5。） |
| 主审核结论 | `approved`（2026-08-16：独立主审核复核跨端点 SQLite 契约回归、JSON 审计零写入、文档状态与 PostgreSQL 未验证待办；无阻断问题。） |
| 任务提交 | `pending` |
| 完成状态 | review_pending：SQLite 契约回归与文档同步完成，等待独立主审核。 |
| 设计基线 | 《公共内容 API 设计》：`610c77cfc927f10564d973c9af4d37c4b38cdf58` |

## 接口变更表

| 变更 | 覆盖接口/状态 | 输入/返回 | 错误 | 兼容性与副作用 | 测试 |
|---|---|---|---|---|---|
| 保持不变 | 三条公共端点与清理 | 已批准设计 -> 验收证据 | 设计定义的 422/404/503 | 不新增接口或写入 | 契约、反向回归、完整检查 |

## 1. 背景

单端点测试不能证明三条公共 API 的字段边界、时区、清理和无副作用语义在组合情况下仍保持一致。版本退出前需要一组跨模块契约回归和明确的验收记录。

## 2. 目标

完成 v1.1.0 的 SQLite 端到端契约回归、完整质量检查和文档状态核对，为 v1.2.0 前端提供可依赖的公共 API 基线。

## 3. 前置依赖

- T1-T4 已完成，且每项任务的精确测试与主审核通过；
- 三条端点、调度清理和统一错误响应已定型；
- 发布库测试可使用 SQLite，生产 PostgreSQL 验证不在本任务中伪造。

## 4. 输入与输出契约

输入：带有公开、隐藏、过期、窗口边界、日报和多来源数据的隔离 SQLite 发布库；FastAPI 测试客户端；不启用真实采集与调度副作用的测试夹具。

输出：

- 三条端点的成功、空结果、参数错误、资源不存在和发布库不可用响应证据；
- 对字段白名单、状态过滤、稳定排序、分页、来源列表和清理状态序列的回归结果；
- 文档中的 T1-T5 状态、实际检查命令和未验证事项与代码事实一致。

## 5. 修改范围

1. 增加或完善跨端点 API 契约和集成测试。
2. 为公共请求无副作用、数据库失败脱敏、清理两次运行和字段反向泄露增加回归测试。
3. 同步 v1.1.0 计划和文档入口中的完成状态、检查记录及未验证项。

## 6. 禁止事项

- 不为通过测试修改已批准的接口、默认值、状态码或字段契约；
- 不顺手新增前端、认证、订阅、管理或部署能力；
- 不将 SQLite 成功描述为 PostgreSQL 或生产部署验证；
- 不修改正式版本号、变更记录或 tag。

## 7. 执行要求

- 先为发现的契约缺口写失败回归测试，再修复对应实现；
- 每项字段隔离测试同时覆盖泄露样本和相邻合法公开字段；
- 清理测试必须记录两次运行中状态、关联数和文章数的变化；
- 所有端点均验证请求前后发布库及 JSON 审计未被查询写入；
- 未能在真实 PostgreSQL 验证的内容登记唯一待办、前置条件和关闭标准，不得标记完成。

### 验收矩阵

| 范围 | 必须覆盖的证据 |
|---|---|
| 日报 | 成功、空/不存在 `404`、非法日期 `422`、不可用 `503`、字段白名单与相邻合法字段、请求零写入 |
| 新闻流 | 十天窗口、来源筛选、两页以上、超末页、非法分页 `422`、不可用 `503`、字段白名单与请求零写入 |
| 来源 | 空列表、第十天保留/第十一天排除、展示名/稳定名排序、禁用来源兼容、不可用 `503`、字段白名单与请求零写入 |
| 清理 | 两次运行逐步断言标记文章、删除关联、删除日报和删除文章的数量变化；失败隔离不改变 ingest 原始结果 |

### 未验证事项登记

实施时必须在 `docs/计划/待办.md` 登记 `WEB-V1.1.0-PG-READ-CLEANUP-01`：在隔离 PostgreSQL 实例运行 `alembic upgrade head` 后，验证三条公共读取端点、十日过期清理事务和高频 ingest 调度集成。关闭条件为记录实例版本、完整命令、端点响应、两轮清理前后计数及调度故障隔离结果。该项不阻塞 T5 的 SQLite 契约回归关闭，但阻塞 `v1.1.0` 标记为版本完成、正式发布或创建 tag。

## 8. 实施步骤

1. 汇总 T1-T4 的契约测试矩阵并补齐缺口。
2. 执行 SQLite FastAPI 集成回归与清理状态序列测试。
3. 执行完整测试、lint、格式和公开边界检查。
4. 核对文档状态、完整 diff 和未验证项，提交版本级主审核。

## 9. 验收标准

1. 三条公共 API 在真实发布库测试数据上符合版本总览的响应契约。
2. 非公开内容和内部字段在组合查询、错误和空结果中均不泄露。
3. 日期、分页、排序和来源筛选在边界输入及两页以上数据下稳定。
4. 清理顺序与两次运行计数均得到验证，查询始终无副作用。
5. `make test`、`make lint`、格式检查和 `make check-public` 全部通过；未验证项明确登记。

## 10. 检查命令

```bash
./venv/bin/pytest tests/test_public_api.py tests/test_publication_store.py tests/test_app_api.py tests/test_ingest_job.py -v
make test
make lint
./venv/bin/ruff format --check .
make check-public
```

### 实施检查记录

- `./venv/bin/pytest tests/test_public_api.py tests/test_publication_store.py tests/test_app_api.py tests/test_ingest_job.py -v`：86 passed；
- `make test`：697 passed、1 skipped（既有人工标注校准门禁）；
- `make lint`、`./venv/bin/ruff format --check .`、`make check-public` 与 `git diff --check`：通过；
- `make task-gate TASK=docs/计划/网站/v1.1.0/T5-契约回归与版本验收.md`：实施前通过；
- PostgreSQL 读取、清理事务和调度集成未执行，已登记 `WEB-V1.1.0-PG-READ-CLEANUP-01`，阻塞版本完成、正式发布和 tag。

## 11. 交付前自检

- 已逐条映射版本退出条件和测试证据；
- 已运行精确与完整检查，结果未被模板或跳过测试伪造；
- 已检查完整 diff、文档状态、敏感信息和范围外改动；
- 已单列未验证的 PostgreSQL/部署验证及其关闭条件。

## 12. 交付格式

按仓库固定交付格式交付；主审核重点为跨端点字段隔离、清理状态序列、查询无副作用、未验证项登记和版本退出条件证据。
