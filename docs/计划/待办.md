# 待办

---
状态: active
最后更新: 2026-08-15
关联:
  - 路线图.md
  - ../架构/来源管理.md
---

## 运行质量

- 继续观察 10 条目标和来源分布，按回放结果调整来源策略。
- 处理 `source_metrics_write_failed` 的来源 key 不一致问题。
- 为推送失败和候选不足增加更直接的告警信息。
- `CG-P3-02-FOLLOWUP-01`：同热点聚合保持 `TOPIC_CLUSTER_ENABLED=0`。待候选池连续保留足量真实候选后，抽取至少 200 对、四类各至少 40 对的跨来源标题/URL，由人工标注“同热点/不同热点”；误聚合率不高于 2%、漏聚合率不高于 15% 后，方可解除校准测试跳过并评估启用开关。该项不阻塞关闭开关下的后续开发。
- `WEB-V1.1.0-PG-READ-CLEANUP-01`：在隔离 PostgreSQL 实例完成 `alembic upgrade head` 后，验证三条公共读取端点、十日过期清理事务与高频 ingest 调度集成。关闭时必须记录实例版本、完整命令、端点响应、两轮清理前后计数及调度故障隔离结果；该项不阻塞 T5 的 SQLite 契约回归关闭，但阻塞 `v1.1.0` 标记版本完成、正式发布或创建 tag。

## 文档治理

- 继续清理历史文档中的失效示例链接，保留原文以便追溯。
- 定期检查正式文档与代码、配置的同步情况。
