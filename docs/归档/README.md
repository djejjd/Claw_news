# 归档说明

这里保存历史探索、任务过程和已被替代的设计。归档内容用于追溯，不作为当前实现依据。

归档中的配置、路径和部署示例仅供追溯，使用前必须以正式文档为准；发现敏感信息或真实环境细节时，应立即停止传播并按公开边界处理。

- `探索/`：早期方案和实验记录。
- `任务/`：历史 Task、设计审查、实施计划和验收材料；开发过程材料也集中放在这里。
- `旧设计/`：被当前架构、运维文档替代的长期设计稿和旧入口。

正式文档入口见上级目录 [docs/README.md](../README.md)。
