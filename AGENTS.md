# Claw_news Agent Development Contract

本文件是所有开发 AI 的仓库级公共契约。共享规则只在此维护；工具专属文件只能补充适配说明，不得复制或改写公共规则。

## 必读顺序

开始任何工作前，依次完整阅读：

1. 用户当前指令与当前任务包；
2. 任务包指定的已批准设计章节；
3. 本文件；
4. 对应工具的专属补充文件；
5. `README.md`、`docs/README.md`、`docs/规范/文档规范.md` 以及任务相关的稳定文档和现有代码。

## 指令优先级

```text
用户当前指令
> 当前任务包
> 已批准设计文档
> AGENTS.md
> CLAUDE.md 中的工具专属补充
> 历史代码注释与归档文档
```

若较低优先级内容与较高优先级内容冲突，以较高优先级为准；满足“强制停止条件”时不得自行裁决后继续实现。

## 强制门禁

以下规则每次修改前必须逐条确认，不可跳过：

1. **中文文档优先**：仓库内设计、计划、开发说明和变更说明默认使用中文；英文只能作为补充，代码标识符、命令和协议字段保持原文。
2. **分支隔离**：任何修改前必须确认不在 `main` 分支；如果在 `main`，必须先创建并切换到新分支，简单修复也不例外。
3. **工作区评估**：多文件改动、功能开发或需要多轮 review 的迭代工作，默认先评估是否应使用独立 worktree 或隔离工作区，不在主工作区长期叠加修改。
4. **任务包启用**：任何代码或测试修改前，当前任务包必须为 `active`，其依赖必须为 `completed` 且主审核结论为 `approved`；必须运行 `make task-gate TASK=<任务包路径>` 并通过。门禁未通过时只能处理任务包或报告阻塞，不得开始实现。
5. **任务级 commit 审核**：任何 `git commit` 前，必须先完整检查并向主审核 AI 展示 `git diff`，取得主审核 AI 的明确批准后方可提交；提交前必须运行 `make task-commit-check TASK=<任务包路径>`。任务包不得以“自行检查 diff”为由绕过该审核。启用 hooks 后，提交命令必须以 `TASK_PATH=<任务包路径> git commit ...` 传入任务包，并在提交说明中包含 `Task: <任务编号>`。用户可直接批准某次 commit，但不是任务级 commit 的必经审批人。
6. **最终集成与外部操作审核**：任何最终 `merge`、`git push` 或 `deploy` 都必须获得用户明确同意，不因改动简单或明显而跳过；主审核 AI 批准任务级 commit 不等于用户批准最终集成、推送或部署。
7. **PR 描述与版本追溯**：创建或更新 PR 必须完整使用 `.github/pull_request_template.md`，如实填写全部章节，不得保留占位符或以临时摘要替代。PR 必须填写目标开发版本、关联开发计划、任务包路径、关联任务或问题；修复类 PR 还必须填写修复归属版本和所修复问题。开发计划版本用于追溯改动归属，必须与 `pyproject.toml`、`CHANGELOG.md` 和 tag 所表示的项目正式发布版本分开说明。

## 版本号规则

- `pyproject.toml` 是项目正式版本的唯一来源，`CHANGELOG.md`、Git tag 和 GitHub Release 必须与其一致。
- 每个 PR 的实施 AI 必须在创建前自动判定并填写模板中的目标开发版本和修复归属版本，不得要求用户逐次指定。任务包或已批准开发计划已指定版本时，直接采用该版本。
- 未指定版本时，实施 AI 按改动语义自动归属：不兼容接口、存储或配置迁移进入下一个 `MAJOR`；向后兼容的新能力进入下一个 `MINOR`；兼容修复和安全修复进入下一个 `PATCH`；纯文档、测试或无行为重构继承关联功能计划版本，无关联计划时标记为“不涉及正式版本发布”。
- 尚未发布的功能和修复继续归属其目标开发版本；已发布版本的修复归属下一个 `PATCH`。Phase 和 Task 是实施里程碑，必须单列关联关系，不得代替开发版本。
- 普通 PR 只更新 PR 描述中的版本归属，不自动修改 `pyproject.toml`、`CHANGELOG.md` 或 tag。仅在版本发布 Task 中，由实施 AI 自动同步正式版本、变更记录、tag 和 Release；创建发布 PR 前必须完成版本计划的退出条件和要求的验证。
- 当任务包、开发计划和改动语义无法唯一确定目标版本时，停止创建 PR 并报告冲突；不得猜测或伪造版本归属。

## 架构与稳定文档入口

- 快速上手：`README.md`
- 文档入口：`docs/README.md`
- 文档规范：`docs/规范/文档规范.md`
- 公开提交边界：`docs/规范/公开边界.md`
- 部署指南：`docs/运维/部署.md`
- 长期架构：`docs/架构/`
- 历史探索：`docs/归档/`，不作为当前实现依据

当前主流程由 `main.py` 编排，数据沿 Collectors → Aggregator → Pusher 流动：

- `collectors/base.py` 定义统一数据对象 `HotItem`；各异步 collector 返回 `List[HotItem]`，由 `safe_collect()` 隔离单源失败。
- `aggregator/` 中的 `Merger.merge()` 负责去重、评分、来源保底、开放竞争和分类截断。
- `pusher/` 中的 `WeComPusher` 负责企微 markdown 格式化与推送。
- `config/settings.py` 负责配置加载与校验；`storage/state_store.py` 负责跨次去重和日报原子落盘。
- `main.py` 负责加载配置、文件锁、采集、合并、推送或 dry-run，以及逐分类状态持久化。

关键约定：`config.yaml` 和 `data/` 不入库；示例配置使用 `config.example.yaml`；测试通过可注入 HTTP client 避免网络；需要 TLS 指纹时使用 `curl-cffi`，企微 webhook 使用 `httpx`。

## 常用检查命令

所有命令使用 `make install` 创建的虚拟环境：

```bash
make install       # 创建 venv 并安装 editable package 与开发依赖
make test          # 运行全部测试：pytest -v
make lint          # Ruff lint 检查
make format        # Ruff 格式化
make dry-run       # 不推送的早间流程，打印格式化结果
make run-morning   # 完整早间流程：collect → score → push
make run-evening   # 完整晚间流程
```

单测示例：

```bash
./venv/bin/pytest tests/test_merger.py::test_function_name -v
```

CI 使用 Python 3.12（本地支持 Python 3.11+），依次运行 `pip install -e ".[dev]"`、`ruff format --check`、`ruff check` 和 `pytest -v`。

## 任务执行规则

每个实施 Task 必须标明任务编号、依赖任务、允许并行任务、预计修改文件、不得修改文件和完成状态，并包含以下十二段：

1. 背景；
2. 目标；
3. 前置依赖；
4. 输入与输出契约；
5. 修改范围；
6. 禁止事项；
7. 执行要求；
8. 实施步骤；
9. 验收标准；
10. 检查命令；
11. 交付前自检；
12. 交付格式。

严格按依赖顺序和文件边界执行。代码 Task 必须测试先行：先看到新测试按预期失败，再写最小实现。新想法默认记录为后续项，不在当前 Task 顺手扩展。每个 Task 只提交自己的范围，任务内未明确授权时不得 commit，任何情况下不得未经用户批准 push。

任务状态和机器门禁的唯一规则见《docs/规范/开发流程.md》。任务从 `ready` 进入 `active` 前，必须由独立启动审查角色核对已批准设计、任务包完整性、依赖、文件边界、并行冲突和分支/worktree，并在任务包记录 `启动审查结论: approved`。随后必须运行 `make task-gate TASK=<任务包>`；“已经阅读任务包”不等于“任务已启用”，未通过不得修改实现或测试文件。该角色按任务调用，不依赖跨会话常驻记忆。

## 代码注释规则

新增或修改代码时，注释默认使用中文；代码标识符、命令、协议字段和第三方 API 名称保持原文。注释必须解释业务意图或约束，而不是逐行复述语法。

以下复杂路径必须补充足以定位决策原因的中文注释：

1. 状态转换、重试、幂等、并发锁和持久化恢复；
2. 失败、降级、回滚和兼容分支的错误语义；
3. 评分、筛选、权限、安全校验和时间边界等不变量；
4. 看似可简化但因历史兼容、外部契约或性能约束而保留的实现。

简单赋值、直观控制流和可由函数名直接理解的代码不得添加无信息量注释。代码 Task 的交付前自检和主审核必须确认复杂新增或修改路径已具备中文定向注释。

## 整改与非功能性变更复审门禁

以下规则适用于修复 review finding，以及修改校验、持久化、审计、统计、回放、配置解析或错误转换的 Task；任一项缺失时不得把对应 finding 标为已关闭。

1. **规则匹配必须双向取样**：每条新增或收紧的字段/字符串/路径匹配规则，测试必须同时覆盖至少一个应拒绝的变体和一个共享词根但应通过的相邻合法值。例如字段名归一化应覆盖 `apiKey`、`x-api-key` 等绕过形式，也应覆盖 `author` 等非认证元数据；不得只测命中样本。
2. **状态和统计必须有变化序列**：新增“每轮”“累计”“前后”计数时，测试输入至少经历两次状态变化，并逐步断言每个计数的分子、分母或前序状态。单轮、空集合或只断言字段存在的测试不能证明计数语义。
3. **持久化读写必须成对验证**：对会写入磁盘的 payload、审计记录和安全校验，测试必须覆盖写入成功/拒绝，以及人工构造历史文件后的读取成功/领域错误；读取异常必须转换为调用方已声明并捕获的领域错误，不能泄漏原始解析或校验异常。
4. **审计事件必须验证契约值**：新增或修改事件时，端到端测试必须验证事件顺序、schema 版本、必填字段及关键值，而非仅断言事件名存在；同一事件写入多个持久化载体时，必须断言两处 payload 完整等值。
5. **整改必须反向验收**：修复每条 review finding 后，实施 AI 必须新增针对原问题的失败测试，并额外检查该修复最可能引入的反向回归（误拒绝、误放行、计数失真、兼容性或错误语义）。交付中单列“整改反向测试”及结果；主审核 AI 复审时优先检查该组测试。
6. **未验证门禁不得伪通过**：依赖真实数据、人工标注、生产权限或外部环境的验收，必须在正式待办登记唯一编号、前置数据、关闭条件、开关状态和是否阻塞后续任务。测试可显式 `skip` 并写明遗留编号，但不得用模板、生成数据或恒等样本报告已通过校准。

## 强制停止条件

若任务包、批准设计、现有代码三者存在无法兼容的冲突，立即停止实现，报告冲突位置、影响和可选处理方案；不得自行扩大范围、修改契约或用静默兼容掩盖冲突。

## 交付前自检

交付前必须逐项确认：

1. 已完整阅读 `AGENTS.md`、任务包和任务指定设计章节；
2. 只修改了允许范围；
3. 没有擅自改变接口、状态语义、默认值或评分常量；
4. 新行为具备正常、边界、降级和失败路径测试；
5. 没有静默 fallback、吞异常或伪成功；
6. 可配置规则没有被重新硬编码到业务逻辑；
7. 旧配置兼容行为符合任务包；
8. 示例配置和中文文档已同步；
9. 已运行任务包指定的精确测试；
10. 已运行要求的完整测试、lint 和格式检查；
11. 已检查 diff 中的意外修改、调试代码、密钥和机器绝对路径；
12. 每条验收标准都有对应证据；
13. 未验证内容明确标记，未被描述为已完成。

不适用于纯文档 Task 的代码测试项，应在交付中明确标记为“不适用”，不得伪造测试结果。

## 固定交付格式

每个开发 AI 必须按以下结构交付：

1. 完成内容；
2. 实际修改文件；
3. 与设计契约的逐条对应；
4. 执行的检查命令及结果；
5. 新增或修改的测试；
6. 未完成项；
7. 残留风险；
8. 发现的设计冲突；
9. 范围外改动声明；
10. 建议主审核 AI 重点检查的位置。
