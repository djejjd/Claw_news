# 变更记录

## v0.2.1 - 2026-08-13

### 修复

- 统一采集、选材、健康检查和发布状态的业务时区。
- 修复带时区偏移的采集时间被错误判断为过期的问题。
- 修复来源指标记录缺失导致已成功飞书推送被标记为 `degraded` 的问题。

### 增强

- 发布状态新增 `selected_count`、`summary_count` 和 `final_count`，可定位条数在哪个阶段减少。
- 飞书推送成功后保留正常 `ok` 状态，来源指标缺失仅记录告警。

### 验证

- 本地测试：523 passed。
- 生产验证：飞书实际推送 10 条，服务状态为 `ok`。
